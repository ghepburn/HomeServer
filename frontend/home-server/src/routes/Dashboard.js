import Navbar from "../navigation/Navbar";

const Dashboard = ({handleClick}) => {
    return(
        <div>
        <Navbar/>
            <p>
                DASHBOARD
            </p>
        </div>
    );
}

export default Dashboard;